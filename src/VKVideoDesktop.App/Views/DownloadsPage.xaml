<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VKVideoDesktop.App.Views.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:VKVideoDesktop.App.ViewModels"
    Background="Transparent">

    <Grid Margin="24,16,24,80">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Загрузки" Style="{StaticResource SubtitleTextBlockStyle}" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="Повторить все" Style="{StaticResource TextButtonStyle}"
                        Click="OnDownloadAllClick" />
                <Button Content="Очистить завершённые" Style="{StaticResource TextButtonStyle}"
                        Click="OnClearCompletedClick" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="1" Text="Активные" Style="{StaticResource SubtitleTextBlockStyle}"
                   Margin="0,0,0,8" Visibility="{x:Bind ViewModel.HasActiveDownloads}" />

        <StackPanel x:Name="EmptyState" Grid.Row="2" HorizontalAlignment="Center"
                    VerticalAlignment="Center" Spacing="12">
            <FontIcon Glyph="&#xE896;" FontSize="48" Opacity="0.3" />
            <TextBlock Text="Нет загрузок" Opacity="0.5" TextAlignment="Center" />
            <TextBlock Text="Скачивайте видео, и они появятся здесь."
                       Opacity="0.4" TextAlignment="Center" FontSize="12" />
        </StackPanel>

        <ListView x:Name="DownloadsList" Grid.Row="2"
                  ItemsSource="{x:Bind ViewModel.Downloads}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:DownloadItemViewModel">
                    <Grid Margin="0,0,0,8" Padding="12"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" Width="120" Height="68" CornerRadius="4"
                              Margin="0,0,12,0">
                            <Image Source="{x:Bind ThumbnailUrl}" Stretch="UniformToFill" />
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Title}" TextWrapping="NoWrap"
                                       TextTrimming="CharacterEllipsis" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind StatusText}" Opacity="0.6" FontSize="12" />
                            <ProgressBar Value="{x:Bind Progress}" Maximum="100"
                                         Margin="0,4,0,4" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Bind ProgressText}" Opacity="0.5" FontSize="11" />
                                <TextBlock Grid.Column="1" Text="{x:Bind SpeedText}" Opacity="0.5" FontSize="11"
                                           Margin="12,0,0,0" />
                                <TextBlock Grid.Column="2" Text="{x:Bind EtaText}" Opacity="0.5" FontSize="11"
                                           Margin="12,0,0,0" />
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="4"
                                    Margin="12,0,0,0">
                            <Button Content="⏸" Click="OnPauseClick"
                                    Tag="{x:Bind DownloadId}"
                                    Visibility="{x:Bind IsDownloading}" />
                            <Button Content="▶" Click="OnResumeClick"
                                    Tag="{x:Bind DownloadId}"
                                    Visibility="{x:Bind IsPaused}" />
                            <Button Content="✕" Click="OnCancelClick"
                                    Tag="{x:Bind DownloadId}" />
                            <Button Content="⋯" Click="OnMoreClick"
                                    Tag="{x:Bind}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

    <Page.Resources>
        <MenuFlyout x:Name="DownloadItemMenuFlyout">
            <MenuFlyoutItem Content="Открыть файл" Click="OnOpenFileClick" />
            <MenuFlyoutItem Content="Открыть папку" Click="OnOpenFolderClick" />
            <MenuFlyoutItem Content="Копировать путь" Click="OnCopyPathClick" />
            <MenuFlyoutSeparator />
            <MenuFlyoutItem Content="Удалить запись" Click="OnDeleteRecordClick" Foreground="Red" />
        </MenuFlyout>
    </Page.Resources>
</Page>
